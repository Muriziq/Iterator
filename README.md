# Iterator
A simple web-based tool that allows users to generate multiple ID cards using a single reusable template. This project is perfect for bulk ID creation—just provide a dataset (like names, titles, photos, etc.), and the app dynamically populates the template for each entry.
